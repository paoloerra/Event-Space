export class Event_like {
    private user_id: number;
    private evento_id: number;

    constructor(user_id: number, evento_id: number ) {
        this.user_id = user_id;
        this.evento_id = evento_id;
    }

    
}